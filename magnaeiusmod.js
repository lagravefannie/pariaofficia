const titleElement = document.querySelector('.title');
const titleAngle = 0; // No rotation

titleElement.style.transform = `rotate(${titleAngle}deg)`;
// The title will appear without any rotation
